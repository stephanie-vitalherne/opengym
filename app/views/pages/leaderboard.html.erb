<% if User.first.first_name != nil %>
<div id="leaderboard"><ol>
  <% @users.each do |u| %><li>
    <%= u.first_name %>
    <%= u.last_name %>

      <% if u.points == nil %>
        0
      <% else %>
        <%= u.points %></li>
    <% end %>
  <% end %></ol>
</div>
  <% end %>

<h1>Basketball Events</h1>

<% @basketball.each do |b| %>
  <div class="events-pages-info">
    <h3><%= link_to "#{b.name}", gym_event_path(@gym, b) %></h3>
    <% if b.description == nil %>
    <% else %>
      <p><%= b.description %></p>
    <% end %>
    <sub>
      <%= b.start_date.strftime("%b. %e, %Y") %>
      -
      <%= b.end_date.strftime("%b. %e, %Y") %>
    </sub>
  </div>
<% end %>

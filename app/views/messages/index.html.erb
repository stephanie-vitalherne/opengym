Your most recent messages

if user_id == current_user.id
show your inbox (list of users you have had messages with, link to their page)
on the other person's user page put an icon to message them
else
show your messages with that user
<br>
this form doesn't work yet, requires sessions
<%= render 'newmessage' %>
mark all read
end

put delete button on message if it's yours and from less than an hour ago

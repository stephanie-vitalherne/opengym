<%= form_for([@user, @message]) do |f| %>
<%# hidden_field :user_id, value: params[:id] %>
<%# hidden_field :sender_id, value: current_user.id %>
<%= f.text_field :subject, placeholder: "Subject" %><br>
<%= f.text_area :body, placeholder: 'Message' %><br>
<%= f.submit "Send" %>
<% end %>
